PROJECT(nuclear-sdk-lv2)

FILE(GLOB HEADERS *.h)
FILE(GLOB SOURCES *.cpp)

IF(CMAKE_HAS_SORT)
  LIST(SORT HEADERS)
  LIST(SORT SOURCES)
ENDIF(CMAKE_HAS_SORT)

ADD_SUBDIRECTORY(common)

INSTALL(FILES ${HEADERS} DESTINATION include/nuclear/sdk/lv2)
