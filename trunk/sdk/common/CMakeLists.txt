PROJECT(nuclear-sdk-common)

FILE(GLOB HEADERS *.h)
FILE(GLOB SOURCES *.cpp)

IF(CMAKE_HAS_SORT)
  LIST(SORT HEADERS)
  LIST(SORT SOURCES)
ENDIF(CMAKE_HAS_SORT)

INSTALL(FILES ${HEADERS} DESTINATION include/nuclear/sdk/common)
